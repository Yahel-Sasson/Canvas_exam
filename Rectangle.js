function Rectangle(
    ribA,
    ribB,
    x,
    y,
    // area,
  ) {
    
    this.ribA = ribA;
    this.ribB = ribB;
    this.x = x;
    this.y = y;
    // this.area = x*y;
  
  }
  
  console.log("Order is loaded!");
